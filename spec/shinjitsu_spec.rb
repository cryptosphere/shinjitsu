require 'spec_helper'

describe Shinjitsu do
  it 'has a version number' do
    expect(Shinjitsu::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
